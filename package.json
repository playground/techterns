{
  "name": "techterns",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "util": "scripts/util.js",
    "tts-test": "npm run util --task=tts",
    "stt-test": "npm run util --task=stt",
    "stt-tts-tokens": "npm run util --task=stt_tts_tokens",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "jsonfile": "^6.1.0",
    "rxjs": "^7.1.0",
    "mqtt": "^4.2.8"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
